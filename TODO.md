# TODO

按性价比排,不是按酷炫程度。第一梯队是「缺了显得不完整」的补齐,不是加法。

---

## 第一梯队 — 补短板

### ① 位置 / 形态记忆　✅ 已完成

**问题**:每次启动跳回右下角,形态(blob/wedge/gem)和线稿也重置。辛苦拖到顺手的位置,重启就没了。

**改哪里**
- `settings.js` — `pet` 段增加 `x` / `y` / `shape` / `sketch`
- `main.js` — `createWindow()` 读存档位置(要校验还在不在屏幕内,显示器拔掉后别把窗口丢到看不见的地方);拖拽结束、`resetPosition`、换形态、切线稿时存盘
- 存盘要节流,散步时窗口每帧都在动,不能每帧写文件

**验收**:拖到左上角 → 换成菱形 → 开线稿 → 重启 → 三样都还在。拔掉外接显示器后重启不会丢窗口。

### ② 打包 + 开机自启

**问题**:现在只能 `npm start`。开源出去别人第一步卡在装 Node。

**改哪里**
- `electron-builder` 配置:macOS `.dmg`(arm64 + x64)、Windows `.exe`(NSIS)
- 图标:从 `assets/tray.png` 生成 `.icns` / `.ico`
- `main.js` — `app.setLoginItemSettings({ openAtLogin })`,设置界面加开关
- macOS 要 `LSUIElement: true`(纯托盘应用,不占 Dock)
- README 补「下载即用」的说明

**验收**:`npm run dist` 产出可双击的安装包;装完勾选开机自启,重启系统后自动出现。

### ③ 拖拽反应 + 自由落体　`进行中`

**问题**:把它拎起来毫无表情,松手直接瞬移落地。物理引擎和表情系统都现成,浪费了。

**改哪里**
- `reactions.js` — 新增 `grabbed` / `dropped` / `shaken` 台词档
- `main.js` — `drag:start` 触发惊讶/挣扎表情;`drag:end` 接管为自由落体(复用 `GRAV` / `REST`),落地压扁弹两下
- 拖拽中快速甩动 → 「别晃了」
- 落体期间不能被行为调度器打断

**验收**:拎起来变惊讶 → 拖到半空松手 → 自由落体 → 落地压扁弹两下 → 回待机。甩几下会抗议。

---

## 第二梯队 — 功能补强

### ④ 双向对话

现在 DeepSeek 是单向的:它评论你,你没法回。点小球弹输入框,打字问它。

> 这个会改变产品定位(状态指示器 → 聊天入口),要处理输入框、多轮上下文、token 成本。
> 建议独立排期,别混在补短板里。

### ⑤ 任务完成的系统通知兜底

桌宠可能被窗口挡住,或在别的桌面/全屏下看不见。全屏写代码时 Claude 干完了根本看不到。
加一条原生通知作为兜底(只在桌宠不可见时发,别双重打扰)。

### ⑥ 并发任务

`agentBusy` 现在是单一状态。Claude 和 Codex 同时跑时,谁先结束谁就把忙碌清掉。
要按 `source` 分别记,全部结束才解除忙碌。

---

## 第三梯队 — 锦上添花

### ⑦ 贴边隐藏

拖到屏幕边缘半隐藏,鼠标靠近再探出来。经典桌宠行为,不占地方。

### ⑧ 换角色

上游仓库的 [mood-mates/](emotion-ball/mood-mates/)(云宝 / 亮亮)是**双许可、可商用**的原创角色,
引擎同源、接口对齐。接进来就能换角色,视觉丰富度翻倍。

### ⑨ 久坐提醒

它已经知道你在干嘛多久了,顺手能做番茄钟 / 护眼提醒。很符合现在这个
「有点脾气的小伙伴」人设。

---

## 已完成

- 表情引擎接入、透明置顶窗、鼠标穿透、拖拽点击
- 物理跳跃 + 落地压缩形变(squash & stretch)
- 自发行为调度(活跃 / 发呆 / 睡眠三段)
- 悬停响应、点击对话、连点闹脾气冷却
- DeepSeek 活动感知评论(应用分类 + MRU 轨迹)
- 图形化设置界面(含安静时段、全屏/游戏免打扰)
- 接入 Claude Code / Codex(一键安装器 + 区分来源 + 干活时不耐烦)
- 跨平台:macOS / Windows 双实现
